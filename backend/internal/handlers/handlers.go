package handlers

// Package handlers: module-specific handlers are defined in separate files
// - `auth_handler.go` contains the current AuthHandler implementation.
// Keep this file minimal while we migrate legacy handlers to the new architecture.
